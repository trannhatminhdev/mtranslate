{
  "name": "mtrans",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered real-time voice translation with virtual microphone",
  "scripts": {
    "dev:be": "pnpm --filter @mtrans/be dev",
    "dev:fe": "pnpm --filter @mtrans/fe dev",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  }
}
